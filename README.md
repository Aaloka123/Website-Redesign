# Website Redesign (AAloka)

React + Vite + TypeScript frontend for the Life Positive Mission website redesign.

## Setup

```bash
cd Frontend
npm install
npm run dev
```

## Build

```bash
cd Frontend
npm run build
```

## Repository

Source lives under `Frontend/`. Dependencies (`node_modules/`) and build output (`dist/`) are not committed.
